package com.example.pharmacyapp;

public interface RecyclerViewInterface {

    //interface is a contract between any class that implements it and just making sure that
    // that class has whatever methods are listed within our interface

    void onItemClick(int position);

}
